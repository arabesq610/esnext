const capitalize = x =>
    x[0].toUpperCase() + x.substr(1)

const doubleSay = x => x + ', ' + x
const exclaim = x => x + '!'

// const x = 'hello' > capitalize

// let y = x

//console.log(x);

const x = exclaim('hello')
//x hello!
